{% extends 'base.html.twig' %}

{% block title %}Cadeau{% endblock %}

{% block body %}

    <div class="mt-3">
        <div class="d-flex justify-content-end">
            <a href="{{ path('app_cadeau_new') }}" class="btn btn-primary">
                Ajouter un nouveau cadeau
            </a>
        </div>
        <table class="table table-striped table-bordered mt-2">
            <thead>
            <tr>
                <th>Nom</th>
                <th>Prix</th>
            </tr>
            </thead>
            <tbody>
            {% for cadeau in cadeaux %}
                <tr>
                    <td>{{ cadeau.nom }}</td>
                    <td>{{ cadeau.prix }} €</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>


{% endblock %}