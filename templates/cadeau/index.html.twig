{% extends 'base.html.twig' %}

{% block title %}Cadeau
{% endblock %}

{% block body %}

	<div class="mt-3">
		<div class="d-flex justify-content-end">
			<a href="{{ path('app_cadeau_new') }}" class="btn btn-primary">
				Ajouter un nouveau cadeau
			</a>
		</div>
		<table class="table table-striped table-bordered mt-2">
			<thead>
				<tr>
					<th>Nom</th>
					<th>Prix</th>
					<th></th>
				</tr>
			</thead>
			<tbody>
				{% for cadeau in cadeaux %}
					<tr>
						<td>{{ cadeau.nom }}</td>
						<td>{{ cadeau.prix }}
							€</td>
						<td>
							<a href="{{ path('app_cadeau_edit', {'id':cadeau.id , 'redirect':'app_cadeau'} )}}" class="btn btn-warning">
								Modifier
							</a>
							<a href="{{ path('app_cadeau_delete', {'id':cadeau.id, 'redirect':'app_cadeau'})}}" class="btn btn-danger">
								Supprimer
							</a>
						</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</div>


{% endblock %}
